"$schema" = 'https://starship.rs/config-schema.json'

format = """
$battery\
$directory\
$custom\
$docker_context\
$env_var\
$jobs\
$status\
$os\
$container\
$cmd_duration\
$line_break\
$sudo\
$character
"""

right_format = """
$nix_shell\
$lua\
$nodejs\
$python\
$rust\
$package\
$time
"""

[[battery.display]]
style = 'bold red'
threshold = 20

[custom.jj]
format = "$output "
shell = ["jj-starship", "--jj-symbol", "  "]
when = "jj-starship detect"

[lua]
format = '[$symbol($version )]($style) '

[nix_shell]
format = '[$symbol$state( \($name\))]($style) '

[nodejs]
format = '[$symbol($version )]($style) '

[package]
format = '[$symbol$version]($style) '
style = "white"

[python]
format = '[${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style) '

[rust]
format = '[$symbol($version )]($style) '

[time]
disabled = false
format = '[$time]($style) '
style = "#7e818b"
