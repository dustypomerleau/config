# tmpl v0.1.0 configuration generated 18 Dec 2023.
# For more information, visit https://github.com/michenriksen/tmpl
---
session:
  name: daily

  ## Uncomment below to run an initial command in windows/panes on creation.
  # on_window: echo 'on_window'
  # on_pane: echo 'on_panel'
  # on_any: echo 'on_any'

  ## Uncomment below to set any additional environment variables.
  # env:
  #  APP_ENV: development
  #  PORT: 8080

  windows:
    - name: shell
      commands:
        - cd /Users/dustinpomerleau/me/audit/
        - jj status
    - name: audit
      commands:
        - cd /Users/dustinpomerleau/me/audit/
        - nvim
    - name: mind
      commands:
        - cd /Users/dustinpomerleau/Library/Mobile\ Documents/iCloud~md\~obsidian/Documents/mind/
        - nvim

    ## Uncomment below to run a shell command in the window on creation.
    # command: echo 'main window'

    ## Uncomment below to run multiple shell commands in the window.
    # commands:
    #   - ssh user@host
    #   - cd /var/logs
    #   - tail -f app.log

    ## Uncomment below to add window panes.
    # panes:
    #   - command: echo 'pane'

## These lines configure editors to be more helpful (optional)
# yaml-language-server: $schema=https://raw.githubusercontent.com/michenriksen/tmpl/main/config.schema.json
# vim: ft=yaml syn=yaml ts=2 sts=2 sw=2 et

